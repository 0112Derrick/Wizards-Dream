export enum EventConstants {
    SIGN_UP = '_sign-up',
    SIGN_IN = '_sign-in',
    LOGOUT = '_logout',
    CHARACTER_CREATE = '_character-create'
}
export enum ServerNameConstants {
    ROOM1 = 'room1',
    ROOM2 = 'room2'
}